// escreva uma funcao que use 2 numeros e retorne o maior entre eles

let valorMaior = max(2, 5)
console.log(valorMaior);

function max(numero1, numero2) {
    // if (numero1 > numero2)
    //     return numero1;
    // return numero2;
    return numero1 > numero2 ? numero1 : numero2;
}